---
description: "Aggregates beer tab data"
enabled: true
entry_point: aggregate.py
pack: beertab
name: aggregate
runner_type: "python-script"
parameters:
  beertabs:
    type: object
    description: "Hash of beer tabs, key is the k/v store key with the, value is the tab for that owner"
  recipient:
    type: string
    description: "Person who is receiving a beer"
      
