---
description: "Aggregates beer tab data"
enabled: true
entry_point: modify.py
pack: beertab
name: modify
runner_type: "python-script"
parameters:
  beertabs:
    type: object
    description: "Hash of beer tabs, key is the owner of the tab, value is the tab for that owner"
  recipient:
    type: string
    description: "Person who is receiving a beer"
      
